/**
 * review feature — constants.
 *
 * The review-model config lives in `<agent-dir>/pi-toolkits-review.json`
 * (same agent-dir convention as the favorites store) and holds a single
 * model key `"provider/id"` — see store.ts.
 */

/** Config file name inside the pi agent dir. */
export const REVIEW_FILE = "pi-toolkits-review.json";

/** Footer status key shown while a review model is configured. */
export const REVIEW_STATUS_KEY = "review";
