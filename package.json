{
  "name": "@andares/pi-toolkits",
  "version": "0.4.3",
  "description": "Integrated toolkit extension for pi — ask mode (read-only Q&A), model favorites, prompt stash, third-party code review, and a dedicated compaction model",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andares/pi-toolkits.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "src"
  ],
  "keywords": [
    "pi",
    "pi-extension",
    "ask-mode",
    "readonly",
    "model-favorites",
    "prompt-stash",
    "code-review",
    "compaction",
    "compact-model",
    "toolkit"
  ],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "dependencies": {
    "@dreki-gg/pi-command-sandbox": "^0.4.3",
    "@earendil-works/pi-tui": "^0.87.1"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.87.1",
    "@earendil-works/pi-coding-agent": "^0.87.1",
    "@types/node": "^26.6.2",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "pnpm typecheck && pnpm test",
    "release": "node ./scripts/publish.mjs",
    "tag-current": "node ./scripts/tag-current.mjs"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
